# 手迹造字 WriteFont - Python 依赖
# git clone 后 pip install -r requirements.txt 即可

# 核心计算
numpy>=1.24.0
scipy>=1.10.0
Pillow>=10.0.0
opencv-python-headless>=4.8.0

# OCR 文字识别（轻量方案，无需 GPU）
pytesseract>=0.3.10

# 字体生成
fonttools>=4.40.0
brotli>=1.0.9

# Web UI
gradio>=4.0.0

# 工具
pyyaml>=6.0
tqdm>=4.65.0
httpx>=0.25.0

# 可选：深度学习（用于本地风格提取和字形生成）
# 取消注释以下行以启用本地 AI 模式：
# torch>=2.0.0
# torchvision>=0.15.0
# scikit-learn>=1.3.0
# matplotlib>=3.7.0
