# 手迹造字 WriteFont - Python 依赖
# git clone 后 pip install -r requirements.txt 即可

# 核心引擎（私有包）
writefont-core>=0.2.0

# Web UI
gradio>=4.0.0

# 工具
pyyaml>=6.0
tqdm>=4.65.0
httpx>=0.25.0

# 可选：深度学习（用于本地风格提取和字形生成）
# 取消注释以下行以启用本地 AI 模式：
# torch>=2.0.0
# torchvision>=0.15.0
# scikit-learn>=1.3.0
# matplotlib>=3.7.0
