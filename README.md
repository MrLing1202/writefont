# 手迹造字 WriteFont

从手写样本到可安装字体，全链路自动化。Android 原生应用，拍照即出字。

## 📥 下载安装

**最新版本：v5.4.0** | [全部版本](https://github.com/MrLing1202/writefont/releases)

| 文件名 | 适用设备 |
|--------|----------|
| `app-arm64-v8a-release.apk` | **主流手机选这个**（推荐） |
| `app-armeabi-v7a-release.apk` | 老款 32 位手机 |
| `app-x86_64-release.apk` | 安卓模拟器 |

> 下载 → 允许安装未知来源 → 安装完成

## ✨ 核心功能

### 📸 拍照造字
- **智能拍照识别** — 对准手写方格纸拍照，AI 自动识别每个字符
- **双引擎投票** — ML Kit + TFLite 两个 AI 引擎独立识别，投票选出最优结果
- **多级预处理投票** — 35+ 种图像预处理策略（含伽马校正/多尺度边缘增强/笔画感知去噪/手写风格自适应）× 多级放大，投票选出最佳识别结果
- **预处理并行化** — Isolate 多核并行预处理（4 核并发），CPU 密集型预处理与 ML Kit 识别形成流水线，识别速度显著提升
- **手写风格自适应** — 自动检测书写风格（楷书/行书/轻笔/重笔），根据风格动态调整预处理策略，行书加强骨架化分离，轻笔加强增粗增强，重笔加强细化处理
- **智能策略组合** — 根据图像特征（对比度/模糊度/笔画粗细）自动选择最优策略子集，越用越聪明
- **多候选综合评分** — 6 维评分（票数+置信度+字频+策略多样性+多尺度一致性+n-gram上下文），不再只看票数
- **候选字面板** — 长按格子查看所有候选字，一键替换识别结果
- **精确置信度** — 0.0~1.0 精确置信度显示，低置信度时自动提示
- **上下文语言模型** — bigram/trigram n-gram 模型，前后文推理预测最可能的字
- **8方向笔画分析** — 横/竖/撇/捺+4个复合方向+曲率分析+复杂度评分，形近字区分更准确
- **智能图像质量评估** — 亮度分布、背景均匀性、笔画连续性自动检测，指导预处理策略选择
- **自适应提前终止** — 根据图片大小动态调整阈值，小图更快，大图更准
- **形近字区分** — 内置 350+ 组形近字对照表，覆盖手写体高频混淆
- **同音字纠错** — 300+ 组同音字 + 500+ 常用词库 + 500+ 三字短语/成语，2-4 字上下文感知自动纠错
- **错误模式学习** — 用户纠正自动记录，高频错误模式自动纠正
- **字典后处理** — 常见字优先，过滤生僻误识别
- **三层缓存** — 精确缓存 + 感知哈希模糊匹配 + 持久化缓存，命中率大幅提升
- **增强置信度校准** — 共识强度、TFLite 一致性、字典验证多维度校准
- **字符边界检测** — 水平+垂直投影分割（粘连字符）+ 断笔合并（距离过近区域自动合并）
- **智能手写区域检测** — 自动检测并移除方格纸网格线，避免干扰字符识别；自动裁剪到手写区域，方格纸只写一部分也能准确定位
- **笔画结构验证** — 识别完成后自动用笔画特征验证结果合理性，相似度过低时从形近字/同音字中选择更匹配的替代
- **图像质量置信度校准** — 根据图片 8 项质量指标微调置信度，高质量图片更自信，低质量图片更保守
- **识别历史记录** — 自动记录识别历史，支持搜索、统计、高频字符分析

### 🔤 字体生成
- **InkForge 引擎** — 自研 TTF 生成引擎，贝塞尔曲线拟合，亚像素级轮廓精度
- **一键生成 TTF** — 3500 字字体 < 3 分钟
- **字体家族生成** — 从一套手写生成 Regular/Bold/Light 多重变体
- **OTF 导出** — 支持 OpenType 格式
- **Google Fonts 格式** — 可直接用于网页嵌入

### ✏️ 字符编辑
- **字符编辑器** — 缩放、旋转、裁剪、笔画粗细调节
- **撤销/重做** — 完整操作历史，支持 50 步回退
- **批量照片导入** — 一次导入多张手写照片，批量识别
- **自定义文本预览** — 输入任意文本实时预览字体效果

### 📊 分析工具
- **字符集完整性分析** — 检查常用字覆盖率，找出缺失字符
- **字形自动补全** — 基于已有字形风格，AI 推断缺失字符
- **字体对比工具** — 并排对比不同字体差异
- **字形评分系统** — 自动评估字形质量
- **Kerning 编辑器** — 调整字符间距
- **风格分析器** — 分析字体风格特征
- **智能字帖生成器** — 分析识别历史中的薄弱字符，自动生成田字格练习字帖，支持导出图片分享
- **识别诊断报告** — 全面的识别统计：置信度分布、薄弱字符分析、趋势追踪、本地 vs 云端对比

### 🎯 识别率优化（本地离线）
- **用户反馈学习** — 纠正过的字符自动记住，下次识别更准
- **多候选 Top-3** — 识别不确定时给出多个候选，用户选择
- **图像质量评估** — 自动检测模糊、低对比度、噪点并增强
- **手写体专用预处理** — 笔画增强、倾斜校正、笔画归一化
- **CLAHE 对比度自适应** — 局部自适应直方图均衡，浅色笔迹也能识别
- **USM 多尺度笔画锐化** — 多尺度 Unsharp Masking 锐化，模糊图片自动增强笔画清晰度
- **智能白边裁剪** — 自动检测并裁剪图片四周空白，让字符占据更大比例，提升识别准确率
- **笔画粗细自适应** — 自动检测笔画宽度，过细膨胀、过粗腐蚀，归一化到最佳识别范围
- **TFLite 本地手写识别** — 内置轻量级手写识别模型，无需联网，与 ML Kit 双引擎投票提升准确率
- **同音字自动纠错** — 利用上下文词组关系，自动纠正"读对但选错"的同音字（如"在/再"、"的/地/得"）
- **粘连字符分割** — 垂直投影法自动检测并切分粘连字符，连笔也能正确识别
- **形态学断笔修复** — 闭运算填充笔画间隙，开运算去噪，细笔画自适应增强
- **策略权重学习** — 预处理策略成功率持久化保存，越用越准，每天自动衰减防止过拟合
- **异常检测过滤** — 自动过滤标点、数字、字母等非汉字结果，识别更干净

### 🌐 其他
- **网页嵌入** — 生成 Web Font 可直接嵌入网页
- **深色模式** — 跟随系统或手动切换
- **多语言** — 中文 / English / 日本語

## 🏗 技术架构

```
┌─────────────────────────────────────────┐
│           Flutter Android App            │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ │
│  │ 拍照识别  │ │ 字符编辑  │ │ 字体生成  │ │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ │
│       │            │            │        │
│  ┌────▼────────────▼────────────▼────┐  │
│  │     识别服务 (多级预处理+投票)      │  │
│  │     ML Kit · 字典 · 笔画分析       │  │
│  └────────────────┬──────────────────┘  │
│                   │                      │
│  ┌────────────────▼──────────────────┐  │
│  │     InkForge TTF 生成引擎          │  │
│  │     贝塞尔拟合 · OpenType 全表     │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

## 📐 使用流程

1. 方格纸手写常用汉字（建议 3500 字）
2. 打开 WriteFont，拍照导入
3. AI 自动识别 → 确认/纠正字符
4. 生成 TTF 字体 → 安装到手机或导出

## 📝 注意事项

- 方格纸网格越清晰，识别越准
- 拍照保持纸张平整、光线均匀
- 纠正过的字符会被记住，越用越准
- 生成的 TTF 全平台通用（Windows/macOS/Linux/Android/iOS）

## 📄 License

[AGPL-3.0](LICENSE) — 可自由使用和修改，禁止闭源商用。

## 🤝 贡献

欢迎提 Issue 和 PR。InkForge 引擎不开源，获取源码请联系作者。
